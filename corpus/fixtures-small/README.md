# fixtures-small

Small public fixtures live here once they pass provenance and privacy review.

The initial Rust tests generate synthetic WAV data at runtime so the repository can validate the corpus path before checked-in signal fixtures exist.
